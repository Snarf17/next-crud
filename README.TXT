
- Nyalakan terlebih dahulu server json 'json-server --watch db.json --port=5050' agar berjalan dilocal server, saya saya menggunakan jsonserver ( install "npm install -g json-server" )terlebih dahulu jika tidak bisa di run 
